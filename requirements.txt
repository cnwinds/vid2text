playwright>=1.40.0
httpx>=0.27.0
yt-dlp>=2024.1.0

# STT core (default: faster-whisper + small)
faster-whisper>=1.0.0

# Optional STT backends — install as needed:
#   pip install openai-whisper torch          # whisper backend
#   pip install -r requirements-stt-optional.txt  # sensevoice (Py3.13 OK with ONNX)
