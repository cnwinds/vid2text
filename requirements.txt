playwright>=1.40.0
httpx>=0.27.0
yt-dlp>=2024.1.0

# STT core (default: SenseVoice ONNX, Chinese-optimized)
funasr-onnx>=0.4.0
modelscope>=1.9.0
jieba>=0.42.0
numpy>=2.0,<2.3

# Optional STT backends:
#   pip install faster-whisper          # fallback, outputs punctuation
#   pip install openai-whisper torch    # whisper backend
