# Optional STT backends — install only what you need
#
# OpenAI Whisper (PyTorch, slower on CPU)
openai-whisper>=20231117
torch

# SenseVoice via ONNX runtime (recommended for Chinese; use pre-exported ONNX on 3.13)
funasr-onnx>=0.4.0
modelscope>=1.9.0
jieba>=0.42.0

# Full FunASR stack (Paraformer, SenseVoice, VAD, etc.)
funasr>=1.0.0
modelscope>=1.9.0
